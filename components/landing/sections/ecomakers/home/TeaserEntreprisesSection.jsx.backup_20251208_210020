import { CheckCircle, Clock, Shield, CreditCard, ArrowRight } from 'lucide-react'
import { Button } from '@/components/ui/button'
import Link from 'next/link'

const avantages = [
  {
    icon: CheckCircle,
    title: "Accès aux profils qui ne postulent pas",
    description: "2000+ professionnels GTB/GTC d'Île-de-France dans notre réseau, dont 60% en veille active."
  },
  {
    icon: Clock,
    title: "2 profils qualifiés en 7 jours",
    description: "Ou -50% sur les honoraires. Nous activons un réseau existant, pas de recherche from scratch."
  },
  {
    icon: CreditCard,
    title: "Paiement au succès uniquement",
    description: "20% du salaire annuel brut, facturés uniquement à l'embauche effective."
  },
  {
    icon: Shield,
    title: "Garantie remplacement 3 mois",
    description: "Si le candidat part dans les 3 mois, remplacement gratuit."
  }
]

export default function TeaserEntreprisesSection() {
  return (
    <section className="w-full py-12 md:py-24 lg:py-32 bg-primary/5" id="entreprises-teaser">
      <div className="container px-4 md:px-6">
        <div className="flex flex-col items-center justify-center space-y-4 text-center mb-12">
          <div className="space-y-2">
            <div className="inline-block rounded-lg bg-primary/10 px-3 py-1 text-sm text-primary font-medium">
              Entreprises
            </div>
            <h2 className="text-3xl font-bold tracking-tighter sm:text-5xl">
              Recrutez les meilleurs talents GTB/GTC
            </h2>
            <p className="max-w-[700px] text-muted-foreground md:text-xl/relaxed">
              Analyse de marché personnalisée gratuite sous 24h
            </p>
          </div>
        </div>
        
        <div className="mx-auto grid max-w-4xl gap-6 md:grid-cols-2">
          {avantages.map((item, index) => {
            const Icon = item.icon
            return (
              <div className="flex gap-4 p-4 rounded-lg border bg-card hover:border-primary transition-colors" key={index}>
                <div className="flex h-10 w-10 shrink-0 items-center justify-center rounded-full bg-primary/10">
                  <Icon className="h-5 w-5 text-primary" />
                </div>
                <div>
                  <h3 className="font-semibold mb-1">{item.title}</h3>
                  <p className="text-sm text-muted-foreground">{item.description}</p>
                </div>
              </div>
            )
          })}
        </div>

        <div className="flex justify-center mt-10">
          <Button asChild size="lg">
            <Link href="/entreprises">
              Obtenir mon analyse gratuite
              <ArrowRight className="ml-2 h-4 w-4" />
            </Link>
          </Button>
        </div>
      </div>
    </section>
  )
}
